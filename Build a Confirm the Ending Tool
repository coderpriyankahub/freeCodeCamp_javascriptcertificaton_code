let a = "Walking on water and developing software from a specification are easy if both are frozen";
let b = "specification";
function confirmEnding(a,b) {
   let stringEnding = a.slice(-b.length);
  if (stringEnding === b) {
    return true;
  }
  return false;
}
console.log(confirmEnding(a,b));
